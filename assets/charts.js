function escapeHtml(str){return String(str).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function barChart(container,data){const width=720,height=330,margin={l:120,r:40,t:20,b:32};const max=Math.max(1,...data.map(d=>d.value));const rowH=(height-margin.t-margin.b)/data.length;let svg=`<svg viewBox="0 0 ${width} ${height}" role="img" aria-label="bar chart">`;data.forEach((d,i)=>{const y=margin.t+i*rowH+6,w=d.value/max*(width-margin.l-margin.r);svg+=`<text x="${margin.l-10}" y="${y+rowH*.55}" text-anchor="end" class="tick">${escapeHtml(d.label)}</text><rect x="${margin.l}" y="${y}" width="${w}" height="${Math.max(13,rowH-10)}" rx="5" class="bar"></rect><text x="${margin.l+w+8}" y="${y+rowH*.55}" class="tick">${Number(d.value).toFixed(1)}%</text>`});svg+='</svg>';container.innerHTML=svg}function lineChart(container,series){const width=720,height=330,margin={l:48,r:28,t:22,b:40};if(!series.length){container.innerHTML="<p class='muted'>No history yet.</p>";return}const values=series.map(x=>x.value);const max=Math.max(1,...values),min=Math.min(0,...values);const innerW=width-margin.l-margin.r,innerH=height-margin.t-margin.b;const pts=values.map((v,i)=>[margin.l+(series.length===1?innerW/2:i/(series.length-1)*innerW),margin.t+(max-v)/(max-min||1)*innerH]);let svg=`<svg viewBox="0 0 ${width} ${height}" role="img" aria-label="line chart">`;for(let i=0;i<=4;i++){const y=margin.t+i*innerH/4;svg+=`<line x1="${margin.l}" y1="${y}" x2="${width-margin.r}" y2="${y}" class="grid-line"/>`}svg+=`<polyline points="${pts.map(p=>p.join(',')).join(' ')}" fill="none" stroke="#2563EB" stroke-width="3"/>`;pts.forEach((p,i)=>svg+=`<circle cx="${p[0]}" cy="${p[1]}" r="4" fill="#2563EB"><title>${escapeHtml(series[i].label)}: ${values[i].toFixed(1)}%</title></circle>`);svg+='</svg>';container.innerHTML=svg}